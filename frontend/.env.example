# Copy this file to `.env` and fill in your values, then restart `npm run dev`.

# Google OAuth Client ID (from Google Cloud Console → Credentials).
# Same value as GOOGLE_CLIENT_ID in backend/.env.
VITE_GOOGLE_CLIENT_ID=

# Backend URL for production builds. Leave blank in dev (Vite proxies /api).
VITE_API_URL=
